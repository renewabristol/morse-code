// add an event listener to the text box you type the text you wish to TRANSLATE in
document.getElementById("translateFrom").addEventListener("change", translate);

// define the function
const translate = () => {};

// output event.target.value to the console to see if event listener works
console.log();

// print the event.target.value to the screen using getelementbyid

//
